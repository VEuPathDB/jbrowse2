
# data dir is wherever the track files live
MACHINE_FILES_DIR=/home/rdoherty/work/services/jbrowse2/sample_files

# port on which service should run (optional; defaults to 8080)
#JBROWSE2_SERVER_PORT=8080

# dockerhub project (set to jbrowse2 to use locally built image; defaults to veupathdb/jbrowse2)
JBROWSE2_IMAGE_NAME=jbrowse2
